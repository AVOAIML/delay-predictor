# APP_ENV=staging — PHASE 2 SEAM (not built). Same shape as dev-azure.env with
# staging Azure resource names. Emitted by infra/ Bicep in Phase 2.
APP_ENV=staging
# (see dev-azure.env for the full field list + verify-before-building TODOs)
