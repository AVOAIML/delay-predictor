# APP_ENV=prod — PHASE 2 SEAM (not built). Same shape as dev-azure.env with
# production Azure resource names + Key Vault secret refs. Emitted by infra/.
APP_ENV=prod
# (see dev-azure.env for the full field list + verify-before-building TODOs)
