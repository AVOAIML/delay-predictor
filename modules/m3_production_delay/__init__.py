"""M3 — Production Delay (advisory). Under construction: only
``llm_agents.weight_agent`` is implemented so far; the rule system,
evaluation agent, risk engine and calibration layer are not yet built.
"""
