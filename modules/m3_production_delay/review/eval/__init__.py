"""Evaluation set for the review judge.

``judge_cases.json`` is built by ``build_judge_cases.py`` from real evidence
packs; ``run_judge_eval.py`` scores a real LLM against it and is inert unless
``M3_REVIEW_JUDGE_EVAL`` is set.
"""
