"""Recorded Risk Engine output used by the Section 3 tests.

``section1_job.json`` is a real ``calculate_delay_elements_for_jobs()`` result,
not a hand-written approximation of one — regenerate it with
``build_fixture.py`` whenever the scenario it records should change.
"""
