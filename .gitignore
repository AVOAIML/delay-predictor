# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / frontend ---
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
frontend/vite.config.ts.timestamp-*.mjs
npm-debug.log*
frontend
CLAUDE.md


# --- Data & ML artifacts (regenerated by make targets — never commit data) ---
dataset/
lake/
reports/
artifacts/
mlruns/
mlflow.db
mlflow_local.db
*.db
*.sqlite
*.sqlite3

# --- Env / secrets (profiles in config/profiles/ are tracked; local overrides are not) ---
.env
.env.local
.env.*.local

# --- Editors / OS ---
.DS_Store
.vscode/
.idea/

# --- Logs ---
*.log

# session scratch (safe to delete)
_to_delete/
_claude_gate_variant/
_prisma/

docs/ai-ml-features/ML_MODEL_CONFIGURATOR_BACKEND_CHANGES.md
modules/m3_delay
modules/m4_bom
modules/m5_scheduling

frontend
CLAUDE.md

# Local-only developer tooling: kept on disk, deliberately not committed.
# Listed here rather than merely untracked so a `git add -A` cannot quietly
# put them back — including the Postman environment, which carries a real
# bearer token.
compose.local.yml
.env.local.example
postman/
scripts/seed_m3_demo.py
scripts/dev_auth.py
scripts/probe_foundry.py
scripts/check_llm.py
